# Collector agent generating simulated cost trends & cloud configurations
from sqlalchemy.orm import Session

def seed_db_mock_metrics(db: Session):
    """Seed DB tables with 90 days of cost histories and resource topologies."""
    # Data seeding scripts
    pass
